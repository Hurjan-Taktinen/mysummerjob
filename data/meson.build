subdir('shaders')

# Copy config file to build directory
configure_file(
  input : 'config.ini',
  output : 'config.ini',
  copy : true)

# configure_file(
  # input : 'models/*',
  # output : 'models/',
  # copy : true)

